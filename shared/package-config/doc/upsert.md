# prompt

## tasks

1. first task is a packgage/apply.json. we want to build this in small steps.
   a. create. checks if the @future-fuze/config package is installed for project in $CWD and installs if not
   b. auto-determine if the invoking tool is npm or pnpm and switch to whichever tool for the package install
   c. add `update` option to apply, that
   d. create a
1. add an update option (default false) that checks to see if the version is latest available updates if not.
   k
1. create an tsconfig/apply.ts file that can be invoked with npx to apply the shared/config/tsconfig file to a project. jj commit that.

## future tasks

- add batch apply to apply multpole & recursive

## notes

jj commit after each step, big or small.
