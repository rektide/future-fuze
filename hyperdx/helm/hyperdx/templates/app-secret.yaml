apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: {{ include "hyperdx.namespace" . }}
  labels:
    {{- include "hyperdx.labels" . | nindent 4 }}
    app.kubernetes.io/component: app
type: Opaque
stringData:
  HYPERDX_API_KEY: {{ .Values.app.apiKey }}
